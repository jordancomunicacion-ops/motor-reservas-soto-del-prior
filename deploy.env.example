# Copia este archivo a deploy.env (ignorado por git) y rellena con
# los valores reales de tu servidor antes de ejecutar DESPLEGAR.bat
# o deploy_motor.bat.

REMOTE_USER=root
REMOTE_HOST=tu.servidor.com
REMOTE_PATH=/root/SOTOdelPRIOR/apps/reservas
